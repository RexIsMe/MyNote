package designpattern;

/**
 * 【设计模式GOF 23种，后续还有新增】
 *
 * 设计模式六大原则：
 * 单一职责原则：一个类、对象只做一件事
 * 开闭原则：对扩展开放，对修改关闭
 * 依赖转置：依赖于接口、抽象开发
 * 里氏替换原则：基类（父类）可以出现的地方，子类一定可以出现（替换它的父类）
 * 迪米特原则：类之间应尽量低耦合
 *
 * 设计模式的作用：
 * 1、可维护（只变动修改点）
 * 2、可重用（没有变动的部分不用任何修改可以继续使用）
 * 3、可扩展（有新增时也不用修改未变动的部分，变动集中起来）
 * 4、灵活性好（各部分可灵活组合使用）
 *
 *
 *
 *
 * @Author li zhiqang
 * @create 2021/12/2
 */
public class Common {
}
